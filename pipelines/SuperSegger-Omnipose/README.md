### Command and Parameters for Omnipose:

```
python -m omnipose --dir raw_img --save_png --dir_above --no_npy --in_folders --omni -- pretrained_model bact_phase_omni --cluster --mask_threshold 1 --flow_threshold 0 --diameter 30
```
